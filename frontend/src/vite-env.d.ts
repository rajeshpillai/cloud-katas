/// <reference types="vite/client" />

// Injected at build time (see vite.config.ts `define`).
declare const __APP_VERSION__: string;
// Build date in YYYY-MM-DD (UTC), stamped at build time.
declare const __BUILD_DATE__: string;
